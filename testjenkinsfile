pipeline {
    agent any

    stages {
        stage('Dep install') {
            agent {
                docker {
                    image 'node:14'
                    label 'docker'
                }
            }
            steps {
                sh 'sudo chown -R 995:993 "/.npm'
                sh 'npm install'
            }
        }
    }
}
